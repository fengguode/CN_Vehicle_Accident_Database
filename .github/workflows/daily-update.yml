name: Archived daily publication

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Live report collection and publication moved to the home-hosted vNext service. This workflow intentionally writes nothing."
